---
import Layout from "./Layout.astro";

interface Props {
  title?: string;
}

const { title = "Greenie" } = Astro.props;
---

<Layout title={title}>
  <main class="mx-auto flex min-h-[calc(100vh-72px)] w-full max-w-md flex-col justify-center px-6 py-12">
    <slot />
  </main>
</Layout>
